__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.DS_Store

# SQLite の実データ。実行するたびに変わるのでコミットしない
data/

# 生成元のプロンプト（このリポジトリの成果物ではない）
prompt-test_apps.md

# --------------------------------------------------------------------------
# reports/ と *.xml は絶対に無視しないこと。
# テスト実施台帳は JUnit XML をリポジトリから読むだけで、テストは実行しない。
# ここを無視した瞬間、台帳側で自動項目がすべて「結果なし」になる。
# --------------------------------------------------------------------------
